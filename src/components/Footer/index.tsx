import React, { type ReactElement } from 'react';

const Footer = (): ReactElement => (
  <footer className="cocos__footer">
    <div>Diseño y código por Franco Cagliolo</div>
  </footer>
);

export default Footer;
