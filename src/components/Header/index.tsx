import React, { type ReactElement } from 'react';

const Header = (): ReactElement => (
  <header className="cocos-header">
    <h1>Cocos Capital Challenge</h1>
  </header>
);

export default Header;
