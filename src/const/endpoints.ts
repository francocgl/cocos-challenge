const BASE_HOST = 'https://dummy-api-topaz.vercel.app/';
const SEARCH_URL = `${BASE_HOST}search`;
const INSTRUMENTS_URL = `${BASE_HOST}instruments`;
const PORTFOLIO_URL = `${BASE_HOST}portfolio`;
const ORDERS_URL = ` ${BASE_HOST}orders`;

export { BASE_HOST, INSTRUMENTS_URL, PORTFOLIO_URL, SEARCH_URL, ORDERS_URL };
